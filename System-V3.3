local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- GUI
local gui = Instance.new("ScreenGui")
gui.Parent = game.CoreGui
gui.Name = "zoewar"
gui.ResetOnSpawn = false

-- Fundo estilo cápsula com transparência fosca (Glassmorphism / Translucido)
local main = Instance.new("Frame")
main.Parent = gui
main.Size = UDim2.new(0, 135, 0, 32)
main.Position = UDim2.new(0.5, -67, 0.75, 0)
main.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
main.BackgroundTransparency = 0.4
main.BorderSizePixel = 0

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0)
corner.Parent = main

-- Borda sutil para dar acabamento fosco
local stroke = Instance.new("UIStroke")
stroke.Parent = main
stroke.Color = Color3.fromRGB(255, 255, 255)
stroke.Transparency = 0.7
stroke.Thickness = 1

-- Texto
local title = Instance.new("TextLabel")
title.Parent = main
title.BackgroundTransparency = 1
title.Size = UDim2.new(0.55, 0, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.Font = Enum.Font.GothamBold
title.Text = ".zoewar"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextScaled = true
title.TextXAlignment = Enum.TextXAlignment.Left

-- ON/OFF
local toggle = Instance.new("TextButton")
toggle.Parent = main
toggle.Size = UDim2.new(0, 42, 0, 22)
toggle.Position = UDim2.new(1, -50, 0.5, -11)
toggle.Font = Enum.Font.GothamBold
toggle.TextScaled = true
toggle.TextColor3 = Color3.fromRGB(255, 255, 255)
toggle.BorderSizePixel = 0

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(1, 0)
toggleCorner.Parent = toggle

-- Estado visual
local enabled = false

local function updateUI()
	if enabled then
		toggle.Text = "ON"
		toggle.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
	else
		toggle.Text = "OFF"
		toggle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
	end
end

local function toggleState()
	enabled = not enabled
	updateUI()
end

toggle.MouseButton1Click:Connect(toggleState)

-- Atalho no teclado: Tecla 'E' ativa/desativa no PC
UIS.InputBegan:Connect(function(input, gameProcessed)
	if not gameProcessed and input.KeyCode == Enum.KeyCode.E then
		toggleState()
	end
end)

updateUI()

-- DRAG MOBILE/PC
local dragging = false
local dragInput
local dragStart
local startPos

local function update(input)
	local delta = input.Position - dragStart

	main.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

main.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1
	or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPos = main.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

main.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement
	or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		update(input)
	end
end)

-- SISTEMA NUCLEAR DE BLOQUEIO DE ANIMAÇÕES NO MM2
-- O MM2 roda animações locais dentro dos scripts da Tool equipada (Gingerscope/Gun/Knife).
-- Esta abordagem intercepta e destrói o carregador de animação de ferramentas e desativa scripts locais da arma.

local function killToolAnimations(tool)
	if not tool:IsA("Tool") then return end
	
	-- Mata qualquer script interno da arma que manipule animações
	for _, v in ipairs(tool:GetDescendants()) do
		if v:IsA("LocalScript") then
			v.Disabled = true
		end
	end
	
	-- Intercepta o evento de ativação/equipe da arma
	tool.Equipped:Connect(function()
		if enabled then
			task.spawn(function()
				local char = LocalPlayer.Character
				if char then
					local humanoid = char:FindFirstChildOfClass("Humanoid")
					if humanoid then
						local animator = humanoid:FindFirstChildOfClass("Animator")
						if animator then
							for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
								track:Stop()
								track:AdjustSpeed(0)
							end
						end
					end
				end
			end)
		end
	end)
end

-- Monitora ferramentas atuais e futuras
if LocalPlayer.Character then
	for _, v in ipairs(LocalPlayer.Character:GetChildren()) do
		killToolAnimations(v)
	end
	LocalPlayer.Character.ChildAdded:Connect(killToolAnimations)
end

if LocalPlayer.Backpack then
	for _, v in ipairs(LocalPlayer.Backpack:GetChildren()) do
		killToolAnimations(v)
	end
	LocalPlayer.Backpack.ChildAdded:Connect(killToolAnimations)
end

LocalPlayer.CharacterAdded:Connect(function(char)
	char.ChildAdded:Connect(killToolAnimations)
	LocalPlayer.Backpack.ChildAdded:Connect(killToolAnimations)
end)

-- Monitoramento global do Animator do Humanoid
local function hookAnimator(char)
	local humanoid = char:WaitForChild("Humanoid")
	local animator = humanoid:WaitForChild("Animator")
	
	animator.AnimationPlayed:Connect(function(track)
		if enabled then
			track:Stop()
			track:AdjustSpeed(0)
		end
	end)
end

if LocalPlayer.Character then
	task.spawn(function()
		pcall(function() hookAnimator(LocalPlayer.Character) end)
	end)
end

LocalPlayer.CharacterAdded:Connect(function(char)
	task.spawn(function()
		pcall(function() hookAnimator(char) end)
	end)
end)

