local UIS = game:GetService("UserInputService")
local SoundService = game:GetService("SoundService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer

-- GUI
local gui = Instance.new("ScreenGui")
gui.Parent = game.CoreGui
gui.Name = "zoewar"

local main = Instance.new("Frame")
main.Parent = gui
main.Size = UDim2.new(0,135,0,32)
main.Position = UDim2.new(0.5,-67,0.75,0)
main.BackgroundColor3 = Color3.fromRGB(0,0,0)
main.BackgroundTransparency = 0.5
main.BorderSizePixel = 0

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1,0)
corner.Parent = main

local title = Instance.new("TextLabel")
title.Parent = main
title.BackgroundTransparency = 1
title.Size = UDim2.new(0.55,0,1,0)
title.Position = UDim2.new(0,10,0,0)
title.Font = Enum.Font.GothamBold
title.Text = ".zoewar"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.TextScaled = true
title.TextXAlignment = Enum.TextXAlignment.Left

local toggle = Instance.new("TextButton")
toggle.Parent = main
toggle.Size = UDim2.new(0,42,0,22)
toggle.Position = UDim2.new(1,-50,0.5,-11)
toggle.Font = Enum.Font.GothamBold
toggle.TextScaled = true
toggle.TextColor3 = Color3.fromRGB(255,255,255)
toggle.BorderSizePixel = 0

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(1,0)
toggleCorner.Parent = toggle

-- ==========================================
-- LISTA DE GUNS REAIS DO MM2 (SEM FACAS)
-- ==========================================
local GUNS_LISTA = {
    "Gun", "Luger", "Blue Luger", "Green Luger", "Red Luger",
    "Ginger Luger", "Amerilaser", "Old Glory", "Blaster", "Virtual",
    "Plasmabeam", "Icebeam", "Elderwood Revolver", "Hallowgun",
    "Minty", "Jinglegun", "Eternalcane", "Lugercane", "Cookiecane",
    "Gingermint",
    "Watergun", "Chroma Watergun", "Rainbow Gun", "Flowerwood Gun",
    "Pearlshine", "Waves", "Sands", "Beachy", "Sunset",
    "Sugarcane", "Sugar", "Iceblaster", "Swirly Gun", "Swirly Gun Chroma",
    "Evergun", "Traveler's Gun", "Raygun", "Alienbeam", "Darkshot",
    "Snowcannon", "Cane Gun (2015)", "Cane Gun (2018)", "Vampire Gun",
    "Ghosts (Gun)", "Toxic (Gun)", "Aurora (Gun)", "Candy Swirl (Gun)",
    "Snakebite (Gun)", "Silent Night (Gun)", "Pop Art (Gun)", "Ginger (Gun)",
    "Molten (Gun)", "Magma (Gun)", "Watcher (Gun)", "Starry (Gun)",
    "Wraith (Gun)", "Frostfade Gun", "Arctic Gun", "Xenoshot", "Bauble",
    "Blue Sugar", "Bronze Sugar", "Silver Sugar", "Gold Sugar",
    "Blue Elderwood", "Bronze Elderwood", "Silver Elderwood", "Gold Elderwood",
    "Blue Minty", "Bronze Minty", "Silver Minty", "Gold Minty",
    "Red Iceblaster", "Bronze Iceblaster", "Silver Iceblaster", "Gold Iceblaster",
    "Chroma Traveler's Gun", "Chroma Evergun", "Chroma Raygun",
    "Chroma Alienbeam", "Chroma Watergun", "Chroma Snowcannon",
    "Gingerscope", "Gold Gingerscope", "Blue Gingerscope",
    "Latte (Gun)", "Arctic (Gun)", "Spectral (Gun)", "Ghost (Gun)",
    "Overseer (Gun)", "Predator (Gun)", "Ripper (Gun)", "Splash (Gun)",
    "Aquarium (Gun)", "Cavern (Gun)", "Frostfade (Gun)", "Slime (Gun)",
    "Pine (Gun)", "Cane 2021 (Gun)", "Ornaments (Gun)", "Candy Corn (Gun)",
    "Carved (Gun)", "Gift Bag (Gun)", "Igloo (Gun)", "Stickers (Gun)",
    "Grave (Gun)", "Haunted (Gun)", "Webbed (Gun)", "Apocalypse (Gun)",
    "Infected (Gun)", "Snowman Gun", "Wrapped Gun", "Fragile (Gun)",
    "Nutcracker", "Bluesteel (Gun)", "Adurite (Gun)", "Darkness (Gun)",
    "Clownfish (Gun)", "Coal (Gun)", "Hot Chocolate (Gun)", "Pumpkin (Gun)",
    "Treats (Gun)", "Hologram (Gun)", "Xeno (Gun)", "Cookie (Gun)",
    "Neon (Gun)", "Tree (Gun)", "Stockings (Gun)", "Popsicle (Gun)",
    "Gingerbread (Gun)", "Gingercookie (Gun)", "Biogun", "Musket", "Colt",
    "M9", "Revolver", "Pistol", "Sheriff", "Phaser", "iRevolver",
    "Rupture", "Mummy (Gun)", "Gothic (Gun)", "Hazard (Gun)"
}

-- ==========================================
-- DETECÇÃO: SÓ GUNS DA LISTA
-- ==========================================
local function isGun(tool)
    if not tool or not tool:IsA("Tool") then return false end
    local nome = tool.Name:lower()
    
    for _, gun in ipairs(GUNS_LISTA) do
        local gunLower = gun:lower()
        if nome == gunLower or nome:find(gunLower, 1, true) then
            return true
        end
    end
    
    return false
end

-- ==========================================
-- ANIMAÇÕES BLOQUEADAS (recuo/impacto + reload)
-- ==========================================
local ANIMACOES_BLOQUEAR = {
    -- Recuo / impacto do tiro
    "recoil", "recuo", "kickback", "kick",
    "impact", "impacto", "punch", "push",
    "shoot", "fire", "atirar", "tiro", "shot",
    "gunshot", "fireshot", "shooting",
    "muzzle", "muzzleflash",
    -- Recarregamento
    "reload", "recarreg", "rechamber",
    "chamber", "bolt", "cock", "magazine", "mag",
    "load", "unload"
}

local ANIMACOES_PERMITIR = {
    "idle", "walk", "run", "jump", "fall",
    "climb", "swim", "sit", "hold", "equip",
    "tool", "pose", "death", "die",
    "aim", "aiming", "ready"
}

local function deveBloquear(track)
    local nome = ""
    pcall(function()
        if track.Animation then
            nome = track.Animation.Name:lower()
        end
    end)
    
    if nome == "" then return false end
    
    for _, p in ipairs(ANIMACOES_PERMITIR) do
        if nome:find(p) then return false end
    end
    
    for _, p in ipairs(ANIMACOES_BLOQUEAR) do
        if nome:find(p) then return true end
    end
    
    return false
end

-- ==========================================
-- SISTEMA
-- ==========================================
local soundGunAtivado = false
local gunConnections = {}
local actionSound = nil
local gunCharConnection = nil
local SOUND_ID = "rbxassetid://6968135315"

local ultimaGunTempo = 0
local COOLDOWN_MEMORIA = 5

local function setupGunSystem(character)
    if not soundGunAtivado then return end
    
    local humanoid = character:WaitForChild("Humanoid", 10)
    local animator = humanoid and humanoid:WaitForChild("Animator", 10)
    
    if animator then
        local c1 = animator.AnimationPlayed:Connect(function(track)
            local tempoDesdeGun = os.clock() - ultimaGunTempo
            if tempoDesdeGun <= COOLDOWN_MEMORIA and deveBloquear(track) then
                pcall(function() track:Stop(0) end)
                pcall(function() track:AdjustSpeed(0) end)
                pcall(function() track:AdjustWeight(0, 0) end)
                pcall(function() track.TimePosition = 0 end)
            end
        end)
        table.insert(gunConnections, c1)
    end
    
    local c2 = character.ChildAdded:Connect(function(c) 
        if c:IsA("Tool") and isGun(c) then
            ultimaGunTempo = os.clock()
            if actionSound then actionSound:Play() end
        end 
    end)
    
    local c3 = character.ChildRemoved:Connect(function(c) 
        if c:IsA("Tool") and isGun(c) then
            ultimaGunTempo = os.clock()
            if actionSound then actionSound:Play() end
        end 
    end)
    
    table.insert(gunConnections, c2)
    table.insert(gunConnections, c3)
end

local enabled = false

local function updateUI()
    if enabled then
        toggle.Text = "ON"
        toggle.BackgroundColor3 = Color3.fromRGB(0,255,0)
    else
        toggle.Text = "OFF"
        toggle.BackgroundColor3 = Color3.fromRGB(255,0,0)
    end
end

local function toggleSoundGun()
    enabled = not enabled
    soundGunAtivado = enabled
    
    if soundGunAtivado then
        actionSound = Instance.new("Sound", SoundService)
        actionSound.SoundId = SOUND_ID
        actionSound.Volume = 1
        
        if player.Character then setupGunSystem(player.Character) end
        gunCharConnection = player.CharacterAdded:Connect(setupGunSystem)
    else
        if gunCharConnection then 
            gunCharConnection:Disconnect() 
            gunCharConnection = nil
        end
        for _, c in ipairs(gunConnections) do c:Disconnect() end
        table.clear(gunConnections)
        if actionSound then 
            actionSound:Destroy() 
            actionSound = nil 
        end
    end
    
    updateUI()
end

toggle.MouseButton1Click:Connect(toggleSoundGun)

UIS.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.E then
        toggleSoundGun()
    end
end)

updateUI()

-- DRAG
local dragging = false
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    main.Position = UDim2.new(
        startPos.X.Scale, startPos.X.Offset + delta.X,
        startPos.Y.Scale, startPos.Y.Offset + delta.Y
    )
end

main.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = main.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

main.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)
